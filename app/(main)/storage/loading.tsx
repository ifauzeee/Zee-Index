import Loading from "@/components/Loading";

// Komponen ini akan ditampilkan secara otomatis saat data untuk StoragePage sedang dimuat.
export default function StorageLoading() {
    return <Loading />;
}