# ==============================================================================
# ZEE-INDEX CONFIGURATION TEMPLATE
# ==============================================================================
# Panduan Instalasi:
# 1. Salin file ini menjadi ".env" (untuk production) atau ".env.local" (untuk development).
# 2. Isi nilai variabel di bawah ini sesuai petunjuk.
# 3. Jangan gunakan spasi di sekitar tanda sama dengan (=).
# 4. Pastikan .env tidak ter-commit ke git (sudah ada di .gitignore)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE CONFIGURATION (GOOGLE DRIVE) - WAJIB
# ------------------------------------------------------------------------------
# Konfigurasi utama untuk menghubungkan aplikasi dengan Google Drive API.

# Google OAuth Credentials
# Dapatkan di Google Cloud Console > APIs & Services > Credentials.
# Tipe Aplikasi: Web Application.
# Redirect URI: [URL_APP]/setup (contoh: http://localhost:3000/setup)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Root Folder ID
# ID dari folder Google Drive yang akan menjadi "Root" atau halaman muka aplikasi.
# Ambil dari URL folder Google Drive: drives/folders/[ID_INI]
NEXT_PUBLIC_ROOT_FOLDER_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz

# Refresh Token
# Token jangka panjang untuk akses API tanpa login ulang.
# Token ini didapatkan setelah proses otentikasi awal via halaman "/setup".
# PENTING: Token ini akan di-generate otomatis saat setup, jangan diisi manual
GOOGLE_REFRESH_TOKEN=


# ------------------------------------------------------------------------------
# 2. SECURITY & AUTHENTICATION - WAJIB
# ------------------------------------------------------------------------------
# Pengaturan keamanan sesi pengguna dan enkripsi.

# Secret Key untuk NextAuth (Session Encryption)
# Generate dengan command: openssl rand -base64 32
# PENTING: Gunakan key yang berbeda untuk setiap environment!
NEXTAUTH_SECRET=your-nextauth-secret-key-here-min-32-chars

# URL Aplikasi
# Set ke URL domain publik anda saat deployment.
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Secret Key untuk fitur Share & Download Links
# Digunakan untuk menandatangani URL agar tidak dapat dimanipulasi.
# Generate dengan command: openssl rand -hex 32
SHARE_SECRET_KEY=your-share-secret-key-min-64-chars-hex

# --- FALLBACK ADMIN CREDENTIALS ---
# Akun admin darurat. Berguna jika database belum disetup atau lupa password.
# Email bisa lebih dari satu, pisahkan dengan koma (tanpa spasi).
# PENTING: Ganti dengan email dan password yang aman!
ADMIN_EMAILS="admin@example.com,admin2@example.com"
ADMIN_PASSWORD="SuperSecurePassword123!"


# ------------------------------------------------------------------------------
# 3. DATABASE (KEY-VALUE STORE) - WAJIB untuk Fitur Penuh
# ------------------------------------------------------------------------------
# Database Redis/KV (WAJIB untuk fitur Tag/Label, Caching, dan Protected Folders).
# Tanpa ini, aplikasi tetap jalan tapi beberapa fitur tidak tersedia.
# Support: Vercel KV, Upstash Redis, Redis Cloud.

# Vercel KV / Upstash Redis URL dan Token
# Dapatkan dari dashboard Vercel KV atau Upstash
KV_REST_API_URL=https://your-redis-instance.upstash.io
KV_REST_API_TOKEN=your-redis-token


# ------------------------------------------------------------------------------
# 4. APP CUSTOMIZATION (OPSIONAL)
# ------------------------------------------------------------------------------
# Penyesuaian tampilan dan perilaku aplikasi.

# Nama tampilan untuk Root Folder (misal: "Film", "Dokumen", "Home").
# Akan muncul di sidebar dan breadcrumb
NEXT_PUBLIC_ROOT_FOLDER_NAME="Home"


# ------------------------------------------------------------------------------
# 5. STORAGE & LIMITS (OPSIONAL)
# ------------------------------------------------------------------------------
# Pengaturan kuota dan monitoring penyimpanan.

# Batas kuota penyimpanan visual (dalam GB).
# Kosongkan untuk menggunakan kuota asli dari akun Google Drive.
# Contoh: 15, 100, 2000
STORAGE_LIMIT_GB=

# Ambang batas peringatan penyimpanan penuh (0.90 = 90%).
# Admin akan menerima notifikasi saat penyimpanan melewati batas ini.
# Range: 0.0 - 1.0 (0.80 = 80%, 0.90 = 90%, dst)
STORAGE_WARNING_THRESHOLD=0.90


# ------------------------------------------------------------------------------
# 6. EMAIL SERVICE (SMTP) (OPSIONAL)
# ------------------------------------------------------------------------------
# Diperlukan untuk fitur Lupa Password, Notifikasi Admin, dan Laporan Mingguan.
# Gunakan Gmail App Password atau SMTP service lainnya.

# SMTP Configuration
# Untuk Gmail: smtp.gmail.com:465 (SSL) atau smtp.gmail.com:587 (TLS)
# Untuk Outlook: smtp-mail.outlook.com:587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-16-chars
EMAIL_FROM="Zee Index <noreply@yourdomain.com>"


# ------------------------------------------------------------------------------
# 7. CRON JOBS (OPSIONAL)
# ------------------------------------------------------------------------------
# Pengaturan untuk tugas terjadwal (pembersihan cache, laporan mingguan, dll).

# Secret untuk mengamankan endpoint cron job.
# Generate dengan command: openssl rand -hex 16
# Digunakan untuk validasi request ke /api/cron/*
CRON_SECRET=your-cron-secret-key


# ------------------------------------------------------------------------------
# 8. EXTERNAL INTEGRATIONS (OPSIONAL)
# ------------------------------------------------------------------------------
# Konfigurasi untuk layanan pihak ketiga dan notifikasi sistem.

# Webhook URL (Discord/Slack/Telegram)
# Diperlukan untuk notifikasi sistem (misal: Laporan Copy File, Error Log).
# Format Discord: https://discord.com/api/webhooks/...
# Format Slack: https://hooks.slack.com/services/...
# Kosongkan jika tidak ingin menggunakan notifikasi eksternal.
WEBHOOK_URL=


# ------------------------------------------------------------------------------
# 9. MONITORING & ERROR TRACKING (OPSIONAL)
# ------------------------------------------------------------------------------
# Konfigurasi untuk tracking error dan performa aplikasi dengan Sentry.

# Sentry DSN (Data Source Name)
# Dapatkan dari project settings di Sentry.io
# Format: https://[key]@[org].ingest.sentry.io/[project-id]
NEXT_PUBLIC_SENTRY_DSN=

# Sentry Auth Token (Opsional, untuk upload source maps saat build)
# Dapatkan dari Sentry.io > Settings > Auth Tokens
# SENTRY_AUTH_TOKEN=


# ------------------------------------------------------------------------------
# 10. LEGACY CONFIGURATION (DEPRECATED - Jangan Digunakan Lagi)
# ------------------------------------------------------------------------------
# Variabel di bawah ini disediakan hanya untuk backward compatibility.
# SANGAT DISARANKAN untuk menggunakan Admin Dashboard untuk mengatur hal ini.

# [DEPRECATED] Folder Terproteksi
# Sekarang kelola via: Admin Dashboard > Security & Control > Protected Folders
# Format: "folderId1:password1,folderId2:password2"
# PRIVATE_FOLDER_IDS=

# [DEPRECATED] Manual Shortcut Drives
# Sekarang kelola via: Admin Dashboard > Shared Drives
# Format: "folderId1:Name1,folderId2:Name2"
# NEXT_PUBLIC_MANUAL_DRIVES=


# ==============================================================================
# CHECKLIST KONFIGURASI MINIMAL
# ==============================================================================
# WAJIB untuk aplikasi bisa berjalan:
# [x] GOOGLE_CLIENT_ID
# [x] GOOGLE_CLIENT_SECRET
# [x] NEXT_PUBLIC_ROOT_FOLDER_ID
# [x] NEXTAUTH_SECRET
# [x] NEXTAUTH_URL
# [x] SHARE_SECRET_KEY
# [x] ADMIN_EMAILS
# [x] ADMIN_PASSWORD
#
# WAJIB untuk fitur penuh (Tag, Cache, Protected Folders):
# [x] KV_REST_API_URL
# [x] KV_REST_API_TOKEN
#
# OPSIONAL tapi direkomendasikan:
# [ ] NEXT_PUBLIC_ROOT_FOLDER_NAME
# [ ] STORAGE_LIMIT_GB
# [ ] WEBHOOK_URL (untuk notifikasi)
# [ ] SMTP_* (untuk email)
# [ ] NEXT_PUBLIC_SENTRY_DSN (untuk monitoring)
# ==============================================================================

# ==============================================================================
# TROUBLESHOOTING
# ==============================================================================
# Jika aplikasi tidak berjalan:
# 1. Pastikan semua variable WAJIB sudah diisi
# 2. Jalankan: pnpm dev atau npm run dev
# 3. Buka: http://localhost:3000/setup untuk setup awal
# 4. Cek console/terminal untuk error messages
#
# Untuk bantuan lebih lanjut, lihat README.md atau buka issue di GitHub
# ==============================================================================
