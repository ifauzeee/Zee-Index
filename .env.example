# =====================================================================
# ZEE-INDEX CONFIGURATION TEMPLATE
# =====================================================================
# Panduan:
# 1. Salin file ini dan ubah namanya menjadi ".env" (atau ".env.local" untuk dev).
# 2. Isi nilai variabel sesuai instruksi di komentar.
# 3. Jangan gunakan spasi di sekitar tanda sama dengan (=).
# =====================================================================

# ==========================================
# 1. Google Drive Configuration (WAJIB)
# ==========================================
# Dapatkan Client ID & Secret dari Google Cloud Console (APIs & Services > Credentials).
# Tipe Aplikasi: Web Application.
# Redirect URI: http://localhost:3000/setup (Local) atau https://domain-anda.com/setup (Prod)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ID Folder Google Drive yang akan menjadi "Root" atau halaman utama index Anda.
# Contoh: 1AbCdEfGhIjKlMnOpQrStUvWxYz
NEXT_PUBLIC_ROOT_FOLDER_ID=

# Token refresh untuk akses offline.
# CARA DAPAT: Jalankan project, buka halaman "/setup", ikuti wizard,
# lalu salin token yang muncul di sini (atau biarkan Setup Wizard menyimpannya di Database).
GOOGLE_REFRESH_TOKEN=

# ==========================================
# 2. Keamanan & Autentikasi (WAJIB)
# ==========================================
# String acak untuk enkripsi sesi login.
# Generate via terminal: openssl rand -base64 32
NEXTAUTH_SECRET=

# URL lengkap aplikasi Anda.
# Localhost: http://localhost:3000
# Production: https://nama-project-anda.vercel.app
NEXTAUTH_URL=http://localhost:3000

# String acak untuk menandatangani link share dan download agar aman.
# Bisa isi string acak apa saja.
SHARE_SECRET_KEY=

# ==========================================
# 3. Database (Vercel KV / Redis) (WAJIB)
# ==========================================
# Digunakan untuk menyimpan cache, session, shared drive manual, dan konfigurasi admin.
# Dapatkan dari dashboard Vercel (Storage > KV) atau Upstash.
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# ==========================================
# 4. Admin & Hak Akses (OPSIONAL)
# ==========================================
# Daftar email yang memiliki akses penuh (Admin Dashboard, Hapus File, Manajemen Drive).
# Pisahkan dengan koma tanpa spasi.
ADMIN_EMAILS=admin@example.com,owner@gmail.com

# ID folder yang ingin disembunyikan dari publik (tetap bisa diakses via Admin/Izin khusus).
PRIVATE_FOLDER_IDS=

# ==========================================
# 5. Email / SMTP (OPSIONAL)
# ==========================================
# Diperlukan untuk mengirim OTP Login, Notifikasi Admin, dan Laporan Mingguan.
# Contoh di bawah menggunakan Gmail (perlu App Password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=email-anda@gmail.com
SMTP_PASS=app-password-anda
EMAIL_FROM="Zee Index <no-reply@example.com>"

# ==========================================
# 6. UI & Sistem (OPSIONAL)
# ==========================================
# Nama folder utama yang muncul di breadcrumb navigasi.
NEXT_PUBLIC_ROOT_FOLDER_NAME="Home"

# [SHARED DRIVE / SHORTCUT CONFIG]
# Daftar Shared Drive atau Shortcut Folder yang ingin ditampilkan di Sidebar.
# Format: ID_FOLDER:NAMA_TAMPILAN
# Pisahkan dengan koma.
# Catatan: Sekarang Anda bisa mengelola ini via Admin Dashboard > Shared Drives (disarankan).
# Isi di sini hanya jika Anda ingin konfigurasi statis (tidak bisa diedit di UI Admin).
NEXT_PUBLIC_MANUAL_DRIVES=

# Batas peringatan penyimpanan penuh (0.90 = 90%). Admin akan dapat email jika penuh.
STORAGE_WARNING_THRESHOLD=0.90

# Token rahasia untuk mengamankan CRON jobs (Vercel Cron) seperti pembersihan log/cache.
# Isi dengan string acak.
CRON_SECRET=

# [OPTIONAL] Override visual batas penyimpanan (dalam GB)
# Isi 10 jika paket Anda 10GB, atau 100 jika 100GB.
# Jika kosong, akan mengikuti data dari Google API.
STORAGE_LIMIT_GB=10