# ==============================================================================
# ZEE-INDEX CONFIGURATION TEMPLATE
# ==============================================================================
# Panduan Instalasi:
# 1. Salin file ini menjadi ".env" (untuk production) atau ".env.local" (untuk development).
# 2. Isi nilai variabel di bawah ini sesuai petunjuk.
# 3. Jangan gunakan spasi di sekitar tanda sama dengan (=).
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE CONFIGURATION (GOOGLE DRIVE)
# ------------------------------------------------------------------------------
# Konfigurasi utama untuk menghubungkan aplikasi dengan Google Drive API.

# Google OAuth Credentials
# Dapatkan di Google Cloud Console > APIs & Services > Credentials.
# Tipe Aplikasi: Web Application.
# Redirect URI: [URL_APP]/setup (contoh: http://localhost:3000/setup)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Root Folder ID
# ID dari folder Google Drive yang akan menjadi "Root" atau halaman muka aplikasi.
# Ambil dari URL folder Google Drive: drives/folders/[ID_INI]
NEXT_PUBLIC_ROOT_FOLDER_ID=

# Refresh Token
# Token jangka panjang untuk akses API tanpa login ulang.
# Token ini didapatkan setelah proses otentikasi awal via halaman "/setup".
GOOGLE_REFRESH_TOKEN=


# ------------------------------------------------------------------------------
# 2. SECURITY & AUTHENTICATION
# ------------------------------------------------------------------------------
# Pengaturan keamanan sesi pengguna dan enkripsi.

# Secret Key untuk NextAuth (Session Encryption)
# Generate: `openssl rand -base64 32`
NEXTAUTH_SECRET=

# URL Aplikasi
# Set ke URL domain publik anda saat deployment.
# Localhost: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Secret Key untuk fitur Share & Download Links
# Digunakan untuk menandatangani URL agar tidak dapat dimanipulasi.
# Generate: `openssl rand -hex 32`
SHARE_SECRET_KEY=

# --- FALLBACK ADMIN CREDENTIALS ---
# Akun admin darurat. Berguna jika database belum disetup atau lupa password.
# Email bisa lebih dari satu, pisahkan dengan koma.
ADMIN_EMAILS="admin@example.com"
ADMIN_PASSWORD="secure_password_here"


# ------------------------------------------------------------------------------
# 3. DATABASE (KEY-VALUE STORE)
# ------------------------------------------------------------------------------
# Database Redis/KV (WAJIB untuk fitur Tag/Label dan Caching).
# Support: Vercel KV, Upstash Redis.

KV_REST_API_URL=
KV_REST_API_TOKEN=


# ------------------------------------------------------------------------------
# 4. APP CUSTOMIZATION (OPTIONAL)
# ------------------------------------------------------------------------------
# Penyesuaian tampilan dan perilaku aplikasi.

# Nama tampilan untuk Root Folder (misal: "Film", "Dokumen", "Home").
NEXT_PUBLIC_ROOT_FOLDER_NAME="Home"


# ------------------------------------------------------------------------------
# 5. STORAGE & LIMITS (OPTIONAL)
# ------------------------------------------------------------------------------
# Pengaturan kuota dan monitoring penyimpanan.

# Batas kuota penyimpanan visual (GB).
# Kosongkan untuk menggunakan kuota asli dari akun Google Drive.
STORAGE_LIMIT_GB=

# Ambang batas peringatan penyimpanan penuh (0.90 = 90%).
# Admin akan menerima email notifikasi saat penyimpanan melewati batas ini.
STORAGE_WARNING_THRESHOLD=0.90


# ------------------------------------------------------------------------------
# 6. EMAIL SERVICE (SMTP) (OPTIONAL)
# ------------------------------------------------------------------------------
# Diperlukan untuk fitur Lupa Password, Notifikasi Admin, dan Laporan Mingguan.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=email-anda@gmail.com
SMTP_PASS=app-password-anda
EMAIL_FROM="Zee Index <no-reply@example.com>"


# ------------------------------------------------------------------------------
# 7. CRON JOBS (OPTIONAL)
# ------------------------------------------------------------------------------
# Pengaturan untuk tugas terjadwal (pembersihan cache, laporan mingguan, dll).

# Secret untuk mengamankan endpoint cron job.
# Generate: `openssl rand -hex 16`
CRON_SECRET=


# ------------------------------------------------------------------------------
# 8. EXTERNAL INTEGRATIONS
# ------------------------------------------------------------------------------
# Konfigurasi untuk layanan pihak ketiga dan notifikasi sistem.

# Webhook URL (Discord/Slack/Telegram)
# Diperlukan untuk notifikasi sistem (misal: Laporan Copy File, Error Log).
# Kosongkan jika tidak ingin menggunakan notifikasi eksternal.
WEBHOOK_URL=


# ------------------------------------------------------------------------------
# 9. LEGACY CONFIGURATION (DEPRECATED)
# ------------------------------------------------------------------------------
# Variabel di bawah ini disediakan hanya untuk backward compatibility.
# SANGAT DISARANKAN untuk menggunakan Admin Dashboard untuk mengatur hal ini.

# [DEPRECATED] Folder Terproteksi
# Kelola via: Admin Dashboard > Protected Folders
# PRIVATE_FOLDER_IDS="1xyz...,2abc..."

# [DEPRECATED] Manual Shortcut Drives
# Kelola via: Admin Dashboard > Manual Drives
# NEXT_PUBLIC_MANUAL_DRIVES="1AbCd...:Project X,1XyZ...:Photos"

# ------------------------------------------------------------------------------
# 10. MONITORING (SENTRY)
# ------------------------------------------------------------------------------
# Konfigurasi untuk tracking error dan performa aplikasi.
# Dapatkan DSN dari project settings di Sentry.io.

NEXT_PUBLIC_SENTRY_DSN=

# Auth Token (Opsional, untuk upload source maps saat build)
# SENTRY_AUTH_TOKEN=
