# ==============================================================================
# ZEE-INDEX CONFIGURATION TEMPLATE
# ==============================================================================
# This application runs fully local with Docker - NO external services required!

# 1. GOOGLE DRIVE CONFIGURATION
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
NEXT_PUBLIC_ROOT_FOLDER_ID=
NEXT_PUBLIC_ROOT_FOLDER_NAME="Home"

# 2. SECURITY & AUTHENTICATION
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
SHARE_SECRET_KEY=
ADMIN_EMAILS=
ADMIN_PASSWORD=

# 3. STORAGE (All data stored in-memory, no external DB required)
# Data is cached in memory and persists as long as the container runs
# For production, data is stored in local Redis container (auto-configured in docker-compose)
STORAGE_LIMIT_GB=
STORAGE_WARNING_THRESHOLD=0.90

# 4. EMAIL SERVICE (SMTP) - Optional
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# 5. OPTIONAL SETTINGS
CRON_SECRET=
WEBHOOK_URL=
SKIP_ENV_VALIDATION=0