# ==============================================================================
# ZEE-INDEX CONFIGURATION TEMPLATE
# ==============================================================================
# Installation Guide:
# 1. Copy this file to ".env" (for production) or ".env.local" (for development).
# 2. Fill in the values for the variables below as instructed.
# 3. Do not use spaces around the equals sign (=).
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE CONFIGURATION (GOOGLE DRIVE)
# ------------------------------------------------------------------------------
# Main configuration to connect the application with the Google Drive API.

# Google OAuth Credentials
# Get these from Google Cloud Console > APIs & Services > Credentials.
# Application Type: Web Application.
# Redirect URI: [APP_URL]/setup (example: http://localhost:3000/setup)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Root Folder ID
# The ID of the Google Drive folder that will serve as the "Root" or home page of the application.
# Get it from the Google Drive folder URL: drives/folders/[THIS_ID]
NEXT_PUBLIC_ROOT_FOLDER_ID=

# Refresh Token
# Long-term token for API access without re-login.
# This token is obtained after the initial authentication process via the "/setup" page.
GOOGLE_REFRESH_TOKEN=


# ------------------------------------------------------------------------------
# 2. SECURITY & AUTHENTICATION
# ------------------------------------------------------------------------------
# User session security and encryption settings.

# Secret Key for NextAuth (Session Encryption)
# Generate via terminal: `openssl rand -base64 32`
NEXTAUTH_SECRET=

# Application URL
# Set this to your public domain URL during deployment.
# Localhost: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Secret Key for Share & Download Links
# Used to sign URLs so they cannot be manipulated.
# Generate via terminal: `openssl rand -hex 32`
SHARE_SECRET_KEY=

# --- FALLBACK ADMIN CREDENTIALS ---
# Emergency admin account. Useful if the database is not set up or password is forgotten.
# You can add multiple emails separated by commas.
ADMIN_EMAILS="admin@example.com"
ADMIN_PASSWORD="secure_password_here"


# ------------------------------------------------------------------------------
# 3. DATABASE (KEY-VALUE STORE)
# ------------------------------------------------------------------------------
# Redis/KV Database (REQUIRED for Tag/Label features and Caching).
# Supports: Vercel KV, Upstash Redis.

KV_REST_API_URL=
KV_REST_API_TOKEN=


# ------------------------------------------------------------------------------
# 4. APP CUSTOMIZATION (OPTIONAL)
# ------------------------------------------------------------------------------
# Customize the look and behavior of the application.

# Display name for the Root Folder (e.g., "Movies", "Documents", "Home").
NEXT_PUBLIC_ROOT_FOLDER_NAME="Home"


# ------------------------------------------------------------------------------
# 5. STORAGE & LIMITS (OPTIONAL)
# ------------------------------------------------------------------------------
# Storage quota settings and monitoring.

# Visual storage quota limit (in GB).
# Leave empty to use the actual Google Drive account quota.
STORAGE_LIMIT_GB=

# Storage warning threshold (0.90 = 90%).
# Admins will receive an email notification when storage exceeds this limit.
STORAGE_WARNING_THRESHOLD=0.90


# ------------------------------------------------------------------------------
# 6. EMAIL SERVICE (SMTP) (OPTIONAL)
# ------------------------------------------------------------------------------
# Required for Forgot Password, Admin Notifications, and Weekly Reports features.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="Zee Index <no-reply@example.com>"


# ------------------------------------------------------------------------------
# 7. CRON JOBS (OPTIONAL)
# ------------------------------------------------------------------------------
# Settings for scheduled tasks (cache cleaning, weekly reports, etc.).

# Secret to secure cron job endpoints.
# Generate via terminal: `openssl rand -hex 16`
CRON_SECRET=


# ------------------------------------------------------------------------------
# 8. EXTERNAL INTEGRATIONS
# ------------------------------------------------------------------------------
# Configuration for third-party services and system notifications.

# Webhook URL (Discord/Slack/Telegram)
# Required for system notifications (e.g., File Copy Reports, Error Logs).
# Leave empty if you do not want to use external notifications.
WEBHOOK_URL=


# ------------------------------------------------------------------------------
# 9. LEGACY CONFIGURATION (DEPRECATED)
# ------------------------------------------------------------------------------
# The variables below are provided only for backward compatibility.
# IT IS HIGHLY RECOMMENDED to use the Admin Dashboard to manage these settings.

# [DEPRECATED] Protected Folders
# Manage via: Admin Dashboard > Protected Folders
# PRIVATE_FOLDER_IDS="1xyz...,2abc..."

# [DEPRECATED] Manual Shortcut Drives
# Manage via: Admin Dashboard > Manual Drives
# NEXT_PUBLIC_MANUAL_DRIVES="1AbCd...:Project X,1XyZ...:Photos"

# ------------------------------------------------------------------------------
# 10. MONITORING (SENTRY)
# ------------------------------------------------------------------------------
# Configuration for error tracking and application performance.
# Get the DSN from project settings in Sentry.io.

NEXT_PUBLIC_SENTRY_DSN=

# Auth Token (Optional, for uploading source maps during build)
# SENTRY_AUTH_TOKEN=
