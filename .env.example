# ==========================================
# 1. Google Drive Configuration (Required)
# ==========================================
# Dapatkan dari Google Cloud Console (OAuth Client ID)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ID Folder Google Drive yang ingin dijadikan Root (Beranda)
NEXT_PUBLIC_ROOT_FOLDER_ID=

# Token refresh untuk akses offline. 
# Jika menggunakan Setup Wizard di web, ini akan disimpan di Redis (KV), 
# tapi disarankan tetap diisi di sini untuk kestabilan.
GOOGLE_REFRESH_TOKEN=

# ==========================================
# 2. App Security & Authentication (Required)
# ==========================================
# String acak untuk enkripsi sesi (Generate: openssl rand -base64 32)
NEXTAUTH_SECRET=

# URL aplikasi (http://localhost:3000 untuk lokal, atau domain Vercel Anda)
NEXTAUTH_URL=http://localhost:3000

# String acak untuk menandatangani link share/download
SHARE_SECRET_KEY=

# ==========================================
# 3. Database / Redis (Vercel KV) (Required)
# ==========================================
# Dapatkan dari dashboard Vercel Storage / Upstash
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# ==========================================
# 4. Administration & Access Control (Optional)
# ==========================================
# Daftar email yang memiliki akses Admin (dipisahkan koma)
# Admin bisa melihat dashboard, menghapus file, dll.
ADMIN_EMAILS=admin@example.com,user@example.com

# ID folder yang ingin disembunyikan dari list publik (dipisahkan koma)
# Folder ini tetap bisa diakses jika user punya izin spesifik.
PRIVATE_FOLDER_IDS=

# ==========================================
# 5. Email / SMTP Configuration (Optional)
# ==========================================
# Diperlukan untuk fitur notifikasi admin, file request, dan 2FA
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=email@gmail.com
SMTP_PASS=app_password_here
EMAIL_FROM="Zee Index <no-reply@example.com>"

# ==========================================
# 6. UI & System Configuration (Optional)
# ==========================================
# Nama yang muncul untuk folder root di breadcrumb (Default: "Beranda" / "Home")
NEXT_PUBLIC_ROOT_FOLDER_NAME="My Drive"

# Ambang batas peringatan penyimpanan penuh (0.90 = 90%)
STORAGE_WARNING_THRESHOLD=0.90

# Token rahasia untuk mengamankan endpoint CRON jobs (Vercel Cron)
CRON_SECRET=